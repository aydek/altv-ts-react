export const enum CommandFlags {
    player = 1,
    vip = 2,
    admin = 3,
    dev = 4,
}

export const enum AnimationFlags {
    normal = 0,
    repeat = 1,
    stop_last_frame = 2,
    upperbody_only = 16,
    enable_player_control = 32,
    cancelable = 120,
}
